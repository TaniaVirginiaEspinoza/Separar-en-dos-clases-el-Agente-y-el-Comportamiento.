# Separar-en-dos-clases-el-Agente-y-el-Comportamiento.
a. En la clase Comportamiento tendrán dos métodos action() y done(). En el action() ahí tendrán el print (“Hola Mundo”) y el done en true. b. En la clase Agente en el método setup(); crear un objeto del tipo Comportamiento y van a mandar a llamar el método addBehaviour() mandando el objeto creado como parámetro.
